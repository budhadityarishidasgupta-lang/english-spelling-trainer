# Admin bootstrap (created automatically on first run if not present)
ADMIN_EMAIL=admin@example.com
ADMIN_NAME=Admin
ADMIN_PASSWORD=ChangeMe!123

# Optional GPT (kept same as before; safe to leave blank)
ENABLE_GPT=0
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
